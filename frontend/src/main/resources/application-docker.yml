devprep:
  api:
    base-url: ${API_BASE_URL:http://172.17.0.1:8081}
spring:
  security:
    oauth2:
      client:
        provider:
          keycloak:
            issuer-uri: ${KEYCLOAK_URL:http://172.17.0.1:8443}/realms/${KEYCLOAK_REALM:devprep}
logging:
  level:
    com.devprep: INFO
    org.springframework.security: WARN